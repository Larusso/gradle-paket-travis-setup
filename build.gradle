plugins {
    id "net.wooga.paket" version "0.5.0"
}

version "1.0.0"

publishing {
    repositories {
        nuget {
            name "Test"
            url "https://wooga.jfrog.io/wooga/api/nuget/atlas-nuget-integrationTest"
        }
    }
}

paketPublish {
    publishRepositoryName = "Test"
}